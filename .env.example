# ─── Database ────────────────────────────────────────────────────────────────
# Provided automatically by Railway's MySQL plugin — copy the value from there
DATABASE_URL=mysql://user:password@host:3306/dbname

# ─── Auth ────────────────────────────────────────────────────────────────────
# Random secret string used to sign JWT / session cookies (generate one below)
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=

# ─── OAuth / Manus ───────────────────────────────────────────────────────────
OAUTH_SERVER_URL=
OWNER_OPEN_ID=
VITE_OAUTH_PORTAL_URL=

# ─── Manus Forge (file storage) ──────────────────────────────────────────────
BUILT_IN_FORGE_API_URL=
BUILT_IN_FORGE_API_KEY=

# ─── App identity ────────────────────────────────────────────────────────────
VITE_APP_ID=

# ─── Vite frontend Forge keys (same as server-side Forge, if applicable) ─────
VITE_FRONTEND_FORGE_API_URL=
VITE_FRONTEND_FORGE_API_KEY=

# ─── Xendit Payment Integration ──────────────────────────────────────────────
# Use test keys (xnd_development_...) until Xendit account verification is done
XENDIT_SECRET_KEY=xnd_development_XXXXXXXXXXXXXXXXXXXXXXXX
# Found in Xendit dashboard → Settings → Callbacks → Callback token
XENDIT_WEBHOOK_TOKEN=
# Public URL of this app — used to build Xendit success/failure redirect URLs
FRONTEND_URL=https://voxa.co.id

# ─── Runtime ─────────────────────────────────────────────────────────────────
NODE_ENV=production
# PORT is set automatically by Railway — do not override it here
